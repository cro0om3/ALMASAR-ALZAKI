# 🚀 خطوات رفع المشروع على Vercel - خطوة بخطوة

## الخطوة 1: إعداد Git و GitHub

### 1.1 تهيئة Git في المشروع
```bash
git init
git add .
git commit -m "Initial commit - Ready for deployment"
```

### 1.2 إنشاء Repository على GitHub
1. اذهب إلى [github.com](https://github.com)
2. اضغط على **+** في الأعلى → **New repository**
3. اسم المشروع: `uncle-website-clean` (أو أي اسم تريده)
4. اختر **Private** (خاص) أو **Public** (عام)
5. **لا** تضع علامة على "Initialize with README"
6. اضغط **Create repository**

### 1.3 ربط المشروع بـ GitHub
```bash
git remote add origin https://github.com/YOUR_USERNAME/uncle-website-clean.git
git branch -M main
git push -u origin main
```

---

## الخطوة 2: إنشاء قاعدة بيانات

### خيار 1: Vercel Postgres (الأسهل) ⭐

1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل دخول بحساب GitHub
3. في Dashboard، اضغط على **Storage** من القائمة الجانبية
4. اضغط **Create Database**
5. اختر **Postgres**
6. اختر الخطة **Hobby** (مجاني)
7. اختر المنطقة **Dubai (dub1)** أو الأقرب لك
8. اسم المشروع: `uncle-website-db`
9. اضغط **Create**
10. بعد الإنشاء، اذهب إلى **Settings** → **Connection String**
11. انسخ **Connection String** (سيبدأ بـ `postgresql://...`)
12. **احفظ هذا الرابط** - سنحتاجه لاحقاً

### خيار 2: Supabase (مجاني وممتاز)

1. اذهب إلى [supabase.com](https://supabase.com)
2. اضغط **Start your project**
3. سجل دخول بحساب GitHub
4. اضغط **New Project**
5. اسم المشروع: `uncle-website`
6. كلمة المرور: اختر كلمة مرور قوية واحفظها
7. المنطقة: اختر الأقرب لك
8. اضغط **Create new project**
9. انتظر حتى يكتمل الإنشاء (دقيقتين)
10. اذهب إلى **Settings** (الترس) → **Database**
11. في قسم **Connection string** → **URI**
12. انسخ الرابط (سيبدأ بـ `postgresql://...`)
13. **احفظ هذا الرابط** - سنحتاجه لاحقاً

---

## الخطوة 3: رفع المشروع على Vercel

### 3.1 من Vercel Dashboard

1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط **Add New Project** (أو **New Project**)
3. ستجد قائمة بـ Repositories من GitHub
4. اختر **uncle-website-clean** (المشروع الذي رفعناه)
5. إذا لم يظهر، اضغط **Import Git Repository** وأدخل رابط GitHub

### 3.2 إعدادات المشروع

1. **Project Name**: `uncle-website` (أو أي اسم)
2. **Framework Preset**: Next.js (سيتم اكتشافه تلقائياً)
3. **Root Directory**: `./` (اتركه كما هو)
4. **Build Command**: `npm run vercel-build` (سيتم ملؤه تلقائياً)
5. **Output Directory**: `.next` (اتركه كما هو)
6. **Install Command**: `npm install` (سيتم ملؤه تلقائياً)

### 3.3 إضافة Environment Variables

**قبل الضغط على Deploy**، اضغط **Environment Variables** وأضف:

#### متغير 1: DATABASE_URL
- **Name**: `DATABASE_URL`
- **Value**: الصق الرابط الذي نسخته من قاعدة البيانات
- **Environment**: اختر **Production, Preview, Development** (الكل)

#### متغير 2: NEXT_PUBLIC_APP_URL (اختياري)
- **Name**: `NEXT_PUBLIC_APP_URL`
- **Value**: `https://uncle-website.vercel.app` (سيتم تحديثه بعد الرفع)
- **Environment**: Production فقط

#### متغير 3: OPENAI_API_KEY (اختياري - للذكاء الاصطناعي)
- **Name**: `OPENAI_API_KEY`
- **Value**: مفتاح OpenAI API (إذا كان لديك)
- **Environment**: Production, Preview, Development

### 3.4 الرفع

1. بعد إضافة Environment Variables، اضغط **Deploy**
2. انتظر حتى يكتمل البناء (3-5 دقائق)
3. بعد الانتهاء، ستحصل على رابط مثل: `https://uncle-website.vercel.app`

---

## الخطوة 4: تحديث قاعدة البيانات (Migrations)

### 4.1 تثبيت Vercel CLI (محلياً)

```bash
npm install -g vercel
```

### 4.2 تسجيل الدخول

```bash
vercel login
```
- سيطلب منك فتح المتصفح وتسجيل الدخول

### 4.3 سحب Environment Variables

```bash
cd C:\Users\FHD_Admin\Documents\OneDrive\Projects\uncle-website-clean
vercel env pull .env.local
```

### 4.4 تشغيل Migrations

```bash
npx prisma generate
npx prisma db push
```

أو إذا كنت تستخدم Migrations:

```bash
npx prisma migrate deploy
```

---

## الخطوة 5: التحقق من النجاح

1. افتح الرابط الذي حصلت عليه من Vercel
2. جرب إنشاء Customer جديد
3. جرب إنشاء Quotation جديد
4. تأكد من أن البيانات تُحفظ

---

## 🆘 حل المشاكل

### المشكلة: "Prisma Client not generated"
**الحل:**
```bash
npx prisma generate
```

### المشكلة: "Database connection failed"
**الحل:**
- تأكد من أن `DATABASE_URL` صحيح
- تأكد من أن قاعدة البيانات نشطة
- تأكد من أن Environment Variable موجود في Vercel

### المشكلة: "Build failed"
**الحل:**
- تحقق من Logs في Vercel Dashboard
- تأكد من أن جميع dependencies موجودة في `package.json`

---

## ✅ Checklist النهائي

- [ ] المشروع على GitHub
- [ ] قاعدة بيانات جاهزة (Vercel Postgres أو Supabase)
- [ ] DATABASE_URL محفوظ
- [ ] المشروع مرفوع على Vercel
- [ ] Environment Variables محددة في Vercel
- [ ] تم تشغيل `prisma generate` محلياً
- [ ] تم تشغيل `prisma db push` محلياً
- [ ] التطبيق يعمل على الرابط الجديد

---

## 📞 إذا واجهت مشكلة

1. تحقق من Logs في Vercel Dashboard → Deployments
2. تأكد من أن Environment Variables محددة بشكل صحيح
3. تأكد من أن قاعدة البيانات نشطة
