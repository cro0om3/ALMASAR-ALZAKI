# 🚀 تعليمات الإعداد النهائية - إنشاء الجداول وإضافة البيانات

## 📋 الوضع الحالي

- ✅ جميع الملفات جاهزة
- ✅ `supabase-schema.sql` يحتوي على جميع الجداول
- ✅ `scripts/create-and-seed-via-supabase.js` جاهز لإضافة البيانات
- ⚠️  الاتصال بقاعدة البيانات من Node.js لا يعمل (مشكلة في كلمة المرور أو إعدادات Supabase)

## 🎯 الحل: استخدام Supabase SQL Editor مباشرة

### الخطوة 1: إنشاء الجداول

1. **افتح Supabase SQL Editor:**
   ```
   https://supabase.com/dashboard/project/ebelbztbpzccdhytynnc/sql/new
   ```

2. **انسخ محتوى `supabase-schema.sql`:**
   - افتح الملف: `supabase-schema.sql`
   - انسخ كل المحتوى (Ctrl+A ثم Ctrl+C)

3. **الصق في SQL Editor واضغط "Run"**

4. **تحقق من النجاح:**
   - يجب أن ترى رسالة نجاح
   - جميع الجداول الـ 13 تم إنشاؤها

### الخطوة 2: إضافة البيانات (5 سجلات لكل جدول)

بعد إنشاء الجداول، شغّل:

```bash
node scripts/create-and-seed-via-supabase.js
```

هذا السكريبت سيقوم بـ:
- ✅ إنشاء 5 مستخدمين (بما في ذلك admin بـ PIN: 1234)
- ✅ إنشاء 5 عملاء
- ✅ إنشاء 5 موردين
- ✅ إنشاء 5 مركبات
- ✅ إنشاء 5 موظفين
- ✅ إنشاء 5 عروض أسعار + عناصرها
- ✅ إنشاء 5 فواتير + عناصرها
- ✅ إنشاء 5 أوامر شراء + عناصرها
- ✅ إنشاء 5 إيصالات
- ✅ إنشاء 5 كشوف مرتبات

### الخطوة 3: التحقق

بعد إضافة البيانات، شغّل:

```bash
node scripts/MASTER-SETUP.js
```

سيقوم بالتحقق من أن كل جدول يحتوي على 5 سجلات على الأقل.

## 🔧 معلومات Supabase

- **Project ID**: `ebelbztbpzccdhytynnc`
- **URL**: `https://ebelbztbpzccdhytynnc.supabase.co`
- **SQL Editor**: `https://supabase.com/dashboard/project/ebelbztbpzccdhytynnc/sql/new`
- **Database Password**: `arjW2j8nDyEdWoPq`

## ⚠️ ملاحظات مهمة

1. **RLS معطّل**: جميع الجداول لديها RLS معطّل - الكل يشوف كل شيء (شركة واحدة)
2. **PIN Code**: تسجيل الدخول بـ PIN Code `1234` فقط
3. **DATABASE_URL**: موجود في `.env.local` لكن الاتصال من Node.js لا يعمل حالياً

## 🐛 إذا فشل إضافة البيانات

إذا فشل `scripts/create-and-seed-via-supabase.js`، استخدم SQL مباشرة:

1. افتح `scripts/create-admin-user-direct.sql`
2. انسخ المحتوى والصقه في Supabase SQL Editor
3. اضغط "Run" لإنشاء مستخدم admin

## ✅ النتيجة المتوقعة

بعد اكتمال الخطوات:
- ✅ 13 جدول موجودة
- ✅ 5 سجلات في كل جدول (65 سجل إجمالاً)
- ✅ PIN Code `1234` يعمل لتسجيل الدخول
- ✅ RLS معطّل - الكل يشوف كل شيء
