# ✅ التحقق من اتصال قاعدة البيانات

## 🎯 طرق التحقق من الاتصال:

### 1. استخدام السكريبت المحلي (الأسهل) ⭐

```bash
node test-and-seed-database.js
```

**ما يفعله:**
- ✅ يختبر الاتصال بقاعدة البيانات
- ✅ يتحقق من وجود جميع الجداول
- ✅ يتحقق من وجود مستخدم Admin
- ✅ يضيف بيانات تجريبية (إذا لم تكن موجودة)
- ✅ يختبر جميع الاستعلامات

**النتيجة المتوقعة:**
```
✅ Connection: Working
✅ Tables: All exist
✅ Admin User: Ready (PIN: 1234)
✅ Sample Data: Added
✅ Queries: Working
```

---

### 2. استخدام SQL في Supabase

1. اذهب إلى: **https://supabase.com/dashboard/project/tundlptcusiogiaagsba/sql**
2. انسخ محتوى `verify-database-connection.sql`
3. الصق في SQL Editor
4. اضغط **RUN**

**ما يفعله:**
- ✅ يتحقق من وجود جميع الجداول
- ✅ يتحقق من وجود مستخدم Admin
- ✅ يعرض عدد السجلات في كل جدول
- ✅ يختبر الاتصال

---

### 3. استخدام API من Vercel (بعد الرفع)

بعد رفع المشروع على Vercel:

1. افتح: `https://your-app.vercel.app/api/test-db`
2. ستظهر معلومات عن:
   - ✅ حالة الاتصال
   - ✅ الجداول الموجودة
   - ✅ عدد السجلات
   - ✅ حالة مستخدم Admin

**مثال على النتيجة:**
```json
{
  "success": true,
  "message": "Database connection successful!",
  "database": {
    "connected": true,
    "tables": {
      "total": 13,
      "found": ["users", "customers", ...]
    }
  },
  "adminUser": {
    "exists": true,
    "email": "admin@example.com"
  }
}
```

---

## 📋 السكريبتات المتاحة:

### 1. `test-and-seed-database.js` ⭐
**الاستخدام:** `node test-and-seed-database.js`
- اختبار شامل + إضافة بيانات تجريبية

### 2. `check-user.js`
**الاستخدام:** `node check-user.js`
- التحقق من وجود المستخدمين فقط

### 3. `test-login-api.js`
**الاستخدام:** `node test-login-api.js`
- اختبار PIN Code فقط

### 4. `create-admin-user.js`
**الاستخدام:** `node create-admin-user.js`
- إنشاء/تحديث مستخدم Admin

### 5. `test-db.js`
**الاستخدام:** `npm run db:test`
- اختبار الاتصال والجداول

---

## ✅ Checklist للتحقق:

- [ ] ✅ الاتصال بقاعدة البيانات يعمل
- [ ] ✅ جميع الجداول موجودة (13 جدول)
- [ ] ✅ مستخدم Admin موجود
- [ ] ✅ PIN Code 1234 يعمل
- [ ] ✅ الاستعلامات تعمل
- [ ] ✅ البيانات التجريبية موجودة

---

## 🎯 بعد التحقق:

إذا كانت جميع الاختبارات ناجحة:
- ✅ قاعدة البيانات جاهزة
- ✅ يمكنك تسجيل الدخول بـ PIN: 1234
- ✅ التطبيق جاهز للاستخدام

---

## 🆘 إذا فشل الاختبار:

### المشكلة: "Cannot reach database"
**الحل:**
1. تحقق من `DATABASE_URL` في `.env.local`
2. تحقق من أن Supabase project نشط
3. تحقق من الاتصال بالإنترنت

### المشكلة: "Tables missing"
**الحل:**
1. اذهب إلى Supabase SQL Editor
2. انسخ محتوى `supabase-schema.sql`
3. الصق و RUN

### المشكلة: "Admin user not found"
**الحل:**
```bash
node create-admin-user.js
```

---

**💡 نصيحة:** استخدم `test-and-seed-database.js` أولاً - هو الأكثر شمولية!
