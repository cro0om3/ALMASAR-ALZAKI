# 🔑 كيفية الحصول على Vercel API Token

## 📋 خطوات سريعة:

### 1. اذهب إلى Vercel Tokens
**الرابط:** https://vercel.com/account/tokens

### 2. أنشئ Token جديد
1. اضغط **Create Token**
2. أعطه اسم (مثلاً: `add-env-vars`)
3. اضغط **Create**
4. **انسخ الـ Token فوراً** (لن تتمكن من رؤيته مرة أخرى!)

### 3. أضف Token إلى `.env.local`

افتح ملف `.env.local` وأضف السطر التالي:

```
VERCEL_TOKEN=your_token_here
```

**مثال:**
```
VERCEL_TOKEN=vercel_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 4. شغّل السكريبت

```bash
node add-vercel-env-via-api.js
```

---

## ⚠️ ملاحظات مهمة:

1. **انسخ الـ Token فوراً** - لن تتمكن من رؤيته مرة أخرى!
2. **لا تشارك الـ Token** - إنه سري مثل كلمة المرور
3. **احفظه في مكان آمن** - ستحتاجه للمستقبل

---

## 🎯 بعد إضافة Token:

1. أضف `VERCEL_TOKEN=your_token_here` إلى `.env.local`
2. شغّل: `node add-vercel-env-via-api.js`
3. ستُضاف جميع Environment Variables تلقائياً!

---

**💡 نصيحة:** بعد إضافة Token إلى `.env.local`، أخبرني وسأشغّل السكريبت لك!
