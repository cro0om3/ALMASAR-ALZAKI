# 🔐 تعليمات نظام PIN Code Authentication

## ✅ تم إضافة نظام PIN Code بنجاح!

---

## 📋 ما تم إضافته:

### 1. ✅ نظام PIN Code Authentication
- `lib/data/user-service.ts` - خدمة إدارة المستخدمين
- `contexts/AuthContext.tsx` - إدارة تسجيل الدخول
- `components/auth/LoginForm.tsx` - نموذج تسجيل الدخول بـ PIN Code
- `app/api/auth/login/route.ts` - API لتسجيل الدخول
- `app/api/users/route.ts` - API لإدارة المستخدمين

### 2. ✅ إدارة المستخدمين
- `components/settings/UsersManagement.tsx` - إدارة المستخدمين
- قسم "Users" في Settings
- Admin يقدر ينشئ/يعدل/يحذف المستخدمين

### 3. ✅ الحماية
- `middleware.ts` - حماية الصفحات (يحتاج تسجيل دخول)
- الكل يشوف كل شيء (لا RLS)

---

## 🚀 الخطوات التالية:

### 1. إنشاء مستخدم Admin افتراضي

1. اذهب إلى: **https://supabase.com/dashboard/project/tundlptcusiogiaagsba/sql**
2. انسخ محتوى `create-admin-user.sql`
3. الصق في SQL Editor
4. اضغط **RUN**

**المستخدم الافتراضي:**
- **Email**: `admin@example.com`
- **PIN Code**: `1234`
- **Role**: `admin`

⚠️ **مهم:** بعد تسجيل الدخول، غيّر PIN Code من Settings!

---

### 2. إنشاء مستخدمين جدد

1. سجل دخول كمستخدم Admin
2. اذهب إلى **Settings** → **Users**
3. اضغط **Add User**
4. أضف:
   - Email
   - Name
   - PIN Code
   - Role (admin, manager, user)
5. اضغط **Create User**

---

## 🎯 كيف يعمل:

### تسجيل الدخول:
1. المستخدم يدخل Email و PIN Code
2. النظام يتحقق من PIN Code
3. إذا صحيح → تسجيل دخول
4. `middleware.ts` يتحقق من الجلسة
5. إذا مسجل دخول → يرى الموقع
6. إذا لا → يوجه إلى `/login`

### إدارة المستخدمين:
- **Admin فقط** يقدر ينشئ/يعدل/يحذف المستخدمين
- من Settings → Users
- يمكن تعديل Email, Name, PIN Code, Role

---

## 🔒 الأمان:

- PIN Code يتم hash باستخدام bcrypt
- لا يتم عرض PIN Code في API responses
- Admin فقط يقدر إدارة المستخدمين
- لا يمكن حذف حسابك الخاص

---

## ✅ Checklist:

- [ ] شغّلت `create-admin-user.sql` في Supabase
- [ ] سجلت دخول بـ admin@example.com / 1234
- [ ] غيّرت PIN Code من Settings
- [ ] أنشأت مستخدمين جدد من Settings → Users
- [ ] اختبرت تسجيل الدخول

---

## 📝 ملاحظات:

- **PIN Code**: يمكن أن يكون أي رقم (مثلاً: 1234, 5678, 0000)
- **Roles**: admin, manager, user
- **الكل يشوف كل شيء**: لا يوجد RLS (كما طلبت)
- **Admin فقط**: يقدر ينشئ/يعدل/يحذف المستخدمين

---

**🎉 كل شيء جاهز! الآن الموقع محمي بـ PIN Code!**
