# 🧪 دليل اختبار قاعدة البيانات

## ✅ الموقع يعمل الآن:
**الرابط:** https://almasar-alzaki.vercel.app/

---

## 📋 خطوات التحقق من قاعدة البيانات:

### 1. اختبار إنشاء Customer جديد

1. اذهب إلى: **Customers** من القائمة الجانبية
2. اضغط **"New Customer"** أو **"+"**
3. املأ البيانات:
   - Name: `Test Customer`
   - Email: `test@example.com`
   - Phone: `+971501234567`
   - Address: `Test Address`
   - City: `Dubai`
   - State: `Dubai`
   - Zip Code: `12345`
   - Country: `UAE`
4. اضغط **"Save"** أو **"Create"**
5. **النتيجة المتوقعة:** يجب أن يظهر Customer جديد في القائمة

---

### 2. اختبار إنشاء Quotation جديد

1. اذهب إلى: **Quotations** من القائمة الجانبية
2. اضغط **"New Quotation"** أو **"+"**
3. املأ البيانات:
   - Customer: اختر Customer الذي أنشأته
   - Date: تاريخ اليوم
   - Valid Until: تاريخ بعد شهر
   - أضف Items (منتجات/خدمات)
4. اضغط **"Save"**
5. **النتيجة المتوقعة:** يجب أن يظهر Quotation جديد

---

### 3. اختبار إنشاء Invoice جديد

1. اذهب إلى: **Invoices** من القائمة الجانبية
2. اضغط **"New Invoice"** أو **"+"**
3. املأ البيانات:
   - Customer: اختر Customer
   - Date: تاريخ اليوم
   - Due Date: تاريخ بعد شهر
   - أضف Items
4. اضغط **"Save"**
5. **النتيجة المتوقعة:** يجب أن يظهر Invoice جديد

---

### 4. التحقق من حفظ البيانات

1. بعد إنشاء Customer/Quotation/Invoice
2. **أعد تحميل الصفحة** (F5 أو Ctrl+R)
3. **النتيجة المتوقعة:** يجب أن تبقى البيانات موجودة

---

### 5. التحقق من قاعدة البيانات مباشرة

#### من Neon Dashboard:

1. اذهب إلى: **https://console.neon.tech**
2. اختر مشروع **"ALMASAR-AIZAKI"**
3. اضغط **"SQL Editor"** أو **"Query"**
4. شغّل هذا الاستعلام:
   ```sql
   SELECT * FROM customers LIMIT 10;
   ```
5. **النتيجة المتوقعة:** يجب أن ترى Customers التي أنشأتها

#### أو من Vercel:

1. اذهب إلى **Vercel Dashboard** → **Project Settings** → **Environment Variables**
2. تأكد من وجود `DATABASE_URL`
3. اذهب إلى **Deployments** → **Functions** → **Logs**
4. راقب Logs للتأكد من عدم وجود أخطاء

---

## 🐛 إذا واجهت مشاكل:

### المشكلة: "Cannot connect to database"
**الحل:**
- تأكد من أن `DATABASE_URL` موجود في Vercel Environment Variables
- تأكد من أن `DATABASE_URL` صحيح
- أعد Deploy المشروع

### المشكلة: "Table does not exist"
**الحل:**
- شغّل Migrations:
  ```bash
  npx prisma db push
  ```
  (محلياً بعد سحب Environment Variables)

### المشكلة: البيانات لا تُحفظ
**الحل:**
- تأكد من أن قاعدة البيانات نشطة
- تحقق من Logs في Vercel
- تأكد من أن Prisma Client يعمل

---

## ✅ Checklist النهائي:

- [ ] الموقع يعمل على https://almasar-alzaki.vercel.app/
- [ ] يمكن إنشاء Customer جديد
- [ ] يمكن إنشاء Quotation جديد
- [ ] يمكن إنشاء Invoice جديد
- [ ] البيانات تُحفظ بعد إعادة التحميل
- [ ] لا توجد أخطاء في Console (F12)
- [ ] قاعدة البيانات متصلة بشكل صحيح

---

## 🎉 بعد التأكد من كل شيء:

التطبيق جاهز للاستخدام! 🚀

يمكنك الآن:
- استخدام التطبيق بشكل طبيعي
- مشاركة الرابط مع المستخدمين
- إضافة بيانات حقيقية
