# 🧪 كيفية اختبار قاعدة البيانات

## ✅ بعد إنشاء الجداول من SQL Editor

بعد أن تقوم بإنشاء الجداول من `supabase-schema.sql` في Supabase SQL Editor، يمكنك اختبار الاتصال:

---

## 📋 خطوات الاختبار:

### 1. تأكد من وجود .env.local

أنشئ ملف `.env.local` في مجلد المشروع (إذا لم يكن موجوداً):

```
DATABASE_URL=postgresql://postgres:Fhd%402992692Fhd@db.tundlptcusiogiaagsba.supabase.co:5432/postgres
```

### 2. شغّل الاختبار

#### الطريقة 1: استخدام npm script (الأسهل) ⭐

```bash
npm run db:test
```

#### الطريقة 2: تشغيل مباشر

```bash
node test-db.js
```

---

## ✅ ما الذي يتحقق منه الاختبار:

1. ✅ **الاتصال بقاعدة البيانات** - يتحقق من أن DATABASE_URL صحيح
2. ✅ **وجود جميع الجداول** - يتحقق من وجود 13 جدول:
   - users
   - customers
   - vendors
   - vehicles
   - employees
   - quotations
   - quotation_items
   - invoices
   - invoice_items
   - purchase_orders
   - purchase_order_items
   - receipts
   - payslips
3. ✅ **اختبار الاستعلامات** - يجرب استعلامات على الجداول الرئيسية

---

## 📊 النتيجة المتوقعة:

إذا كان كل شيء يعمل بشكل صحيح، سترى:

```
🔍 Testing Supabase Database Connection...

✅ DATABASE_URL is set
🔌 Connecting to database...

✅ Database connection successful!

📋 Checking tables...

Found 13 tables:

  ✅ users
  ✅ customers
  ✅ vendors
  ✅ vehicles
  ✅ employees
  ✅ quotations
  ✅ quotation_items
  ✅ invoices
  ✅ invoice_items
  ✅ purchase_orders
  ✅ purchase_order_items
  ✅ receipts
  ✅ payslips

📊 Testing queries...

  ✅ Customers table: 0 records
  ✅ Quotations table: 0 records
  ✅ Invoices table: 0 records
  ✅ Employees table: 0 records
  ✅ Vehicles table: 0 records

🎉 All tests passed! Database is ready to use!
```

---

## 🐛 إذا واجهت مشاكل:

### المشكلة: "DATABASE_URL is not set"
**الحل:**
- تأكد من وجود ملف `.env.local`
- تأكد من أن DATABASE_URL موجود في الملف

### المشكلة: "Some tables are missing"
**الحل:**
- اذهب إلى Supabase SQL Editor
- شغّل SQL من `supabase-schema.sql` مرة أخرى

### المشكلة: "Connection failed"
**الحل:**
- تحقق من أن Supabase project نشط
- تحقق من أن Connection String صحيح
- تحقق من اتصال الإنترنت

---

## 🎯 بعد نجاح الاختبار:

✅ قاعدة البيانات جاهزة للاستخدام!
✅ يمكنك الآن:
- إضافة DATABASE_URL في Vercel
- استخدام التطبيق بشكل طبيعي
- إنشاء Customers, Quotations, Invoices

---

**💡 نصيحة:** شغّل `npm run db:test` بعد أي تغيير في قاعدة البيانات للتأكد من أن كل شيء يعمل!
